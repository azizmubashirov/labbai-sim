/**
 * Chart Generator block defaults.
 * Intent/chart rules live in prompts + skills — only operational defaults here.
 */
export const CHART_GENERATOR_DEFAULT_MODEL = 'gpt-5.5'

/** Alternative strong default supported in the model combobox. */
export const CHART_GENERATOR_ALT_MODEL = 'claude-opus-4-8'
